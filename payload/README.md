# Local payload staging

Place a reviewed proxy runtime tree here before running `tools/build.sh`. The
payload itself is ignored by Git and must not contain secrets. This README remains
tracked so the required staging layout is documented.
